@model LearnEnglish.Models.Quiz

@{
    ViewData["Title"] = "Удаленеие теста";
}

<section class="edit-element">
    <div class="container">
        <h2>Вы уверены, чтобы хотите удалить элемент?</h2>

        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a class="breadcrumb-link" asp-controller="Home" asp-action="Index">Главная</a></li>
            <li class="breadcrumb-item"><a class="breadcrumb-link" asp-controller="Quizs" asp-action="Index">Тесты</a></li>
            <li class="breadcrumb-item active">Удаление теста</li>
        </ol>

        <div>
            <dl class="row">
                <dt class = "col-sm-2">
                    @Html.DisplayNameFor(model => model.Article)
                </dt>
                <dd class = "col-sm-10">
                    @Html.DisplayFor(model => model.Article.Title)
                </dd>
                <dt class = "col-sm-2">
                    @Html.DisplayNameFor(model => model.Title)
                </dt>
                <dd class = "col-sm-10">
                    @Html.DisplayFor(model => model.Title)
                </dd>

                <form asp-action="Delete">
                    <input type="hidden" asp-for="Id" />
                    <input type="submit" value="Удалить" class="btn-danger" /> |
                    <a class="back-to-list" asp-action="Index">Вернуться к списку</a>
                </form>
            </dl>
        </div>
    </div>
</section>
    
